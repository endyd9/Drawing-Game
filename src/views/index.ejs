<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Drawing Game</title>
    <link rel="stylesheet" href="/lib/style.css">
</head>

<% let cnt=0; const shuffleArray=(sourceArr)=> {
    const array = sourceArr.concat();
    // 피셔 예이츠 알고리즘
    const arrayLength = array.length;
    for (let i = arrayLength - 1; i >= 0; i--) {
    const randomIndex = Math.floor(Math.random() * (i + 1));
    [array[i], array[randomIndex]] = [array[randomIndex], array[i]];
    }

    return array;
    };
    const shuffled = shuffleArray(keywords);
    %>

    <body>
        <header>
            <h1>VisionAPI's Keyword &darr; </h1>
            <br>
            <h1>
                <%=shuffled[cnt]%>
            </h1>
        </header>

        <canvas></canvas>
        <form method="post">
            <div class="btns">
                <label for="color">
                    Chose Color
                    <input type="color" id="color">
                </label>
                <input id="line-width" type="range" min="1" max="10" value="3" step="0.1">
                <button type="button" id="mode-btn">🪣Fill</button>
                <button type="button" id="reset-btn">❌Reset</button>
                <button type="button" id="eraser-btn">Erase</button>
                <button id="save">Submit</button>
                <input type="text" name="url" class="test" style="display: none">
                <input type="text" name="keyword" value="<%=shuffled[cnt]%>" style="display:none">
            </div>
        </form>
    </body>
    <script src="/lib/index.js"></script>

</html>